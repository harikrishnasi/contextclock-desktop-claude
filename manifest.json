{
  "manifest_version": "0.2",
  "name": "contextclock-desktop",
  "display_name": "ContextClock",
  "version": "1.0.0",
  "description": "Gives Claude Desktop real-time temporal context — time, date, and period of day injected into every conversation.",
  "author": {
    "name": "Your Name"
  },
  "homepage": "https://github.com/yourusername/ContextClock",
  "server": {
    "type": "node",
    "entry_point": "build/index.js",
    "mcp_config": {
      "command": "node",
      "args": [
        "${__dirname}/build/index.js"
      ],
      "env": {
        "REFRESH_INTERVAL_MINS": "${user_config.refresh_interval}"
      }
    }
  },
  "icon": "icon128.png",
  "tools": [
    {
      "name": "get_current_time",
      "description": "Get the current local time, date, and period of day"
    },
    {
      "name": "get_elapsed_time",
      "description": "Calculate human-readable elapsed time since a given ISO timestamp"
    },
    {
      "name": "should_refresh_time",
      "description": "Check if temporal context needs refreshing. Call this before every response."
    },
    {
      "name": "convert_timezone",
      "description": "Convert current local time to a specified IANA timezone (e.g. 'Europe/London')"
    }
  ],
  "prompts": [
    {
      "name": "temporal_context",
      "description": "Inject current temporal context into the conversation",
      "text": "Please keep this temporal context in mind throughout our conversation."
    }
  ],
  "keywords": [
    "time",
    "context",
    "temporal",
    "ai",
    "claude"
  ],
  "license": "MIT",
  "user_config": {
    "refresh_interval": {
      "type": "number",
      "title": "Refresh Interval (Minutes)",
      "description": "How often Claude should automatically refresh its time context.",
      "default": 30,
      "min": 1,
      "max": 1440,
      "required": false
    }
  }
}