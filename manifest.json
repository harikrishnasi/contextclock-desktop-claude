{
  "manifest_version": "0.2",
  "name": "contextclock-desktop",
  "display_name": "ContextClock",
  "version": "1.0.0",
  "description": "By default, Claude Desktop has no concept of the current time. ContextClock fixes this by automatically injecting real-time temporal context into every conversation. It forces Claude to check the time before responding, tracks elapsed time during long chats, converts global timezones, and adapts to your custom working hours and off-days so Claude knows exactly when you're 'on the clock'.",
  "author": {
    "name": "Hari Krishna S I"
  },
  "homepage": "https://github.com/yourusername/ContextClock",
  "server": {
    "type": "node",
    "entry_point": "build/index.js",
    "mcp_config": {
      "command": "node",
      "args": [
        "${__dirname}/build/index.js"
      ],
      "env": {
        "REFRESH_INTERVAL_MINS": "${user_config.refresh_interval}",
        "WORK_START_HOUR": "${user_config.work_start_hour}",
        "WORK_END_HOUR": "${user_config.work_end_hour}",
        "WORK_DAYS": "${user_config.work_days}"
      }
    }
  },
  "icon": "icon128.png",
  "tools": [
    {
      "name": "get_current_time",
      "description": "Get the current local time, date, and period of day"
    },
    {
      "name": "get_elapsed_time",
      "description": "Calculate human-readable elapsed time since a given ISO timestamp"
    },
    {
      "name": "should_refresh_time",
      "description": "Check if temporal context needs refreshing. Call this before every response."
    },
    {
      "name": "convert_timezone",
      "description": "Convert current local time to a specified IANA timezone (e.g. 'Europe/London')"
    }
  ],
  "prompts": [
    {
      "name": "temporal_context",
      "description": "Inject current temporal context into the conversation",
      "text": "Please keep this temporal context in mind throughout our conversation."
    }
  ],
  "keywords": [
    "time",
    "context",
    "temporal",
    "ai",
    "claude"
  ],
  "license": "MIT",
  "user_config": {
    "refresh_interval": {
      "type": "number",
      "title": "Refresh Interval (Minutes)",
      "description": "How often Claude should automatically refresh its time context.",
      "default": 30,
      "min": 1,
      "max": 1440,
      "required": false
    },
    "work_start_hour": {
      "type": "number",
      "title": "Work Start Hour",
      "description": "The hour you start work (0-23, e.g. 9 for 9 AM).",
      "default": 9,
      "min": 0,
      "max": 23,
      "required": false
    },
    "work_end_hour": {
      "type": "number",
      "title": "Work End Hour",
      "description": "The hour you finish work (0-23, e.g. 17 for 5 PM).",
      "default": 17,
      "min": 0,
      "max": 23,
      "required": false
    },
    "work_days": {
      "type": "string",
      "title": "Working Days",
      "description": "Comma-separated list of working days.",
      "default": "Monday, Tuesday, Wednesday, Thursday, Friday",
      "required": false
    }
  }
}